

describe("Application", () => {
    describe("Searchbar", () => {
        beforeEach(() => {
            // setup
        });
        it("should be displayed as 1", () => { 
            let i = 1;
            expect(i).toBe(1);
        });
        afterEach(() => {   
            // teardown
        });
        it("should be displayed as 2", () => {
            expect(1 + 1).toBe(2);
    });
})});
